services:
  sawala:
    build: .
    image: sawala:latest
    ports:
      - "8000:8000"
    volumes:
      - sawala-data:/app/data
      - sawala-logs:/app/logs
    restart: unless-stopped

volumes:
  sawala-data:
  sawala-logs:
